import { notFound } from "next/navigation";
import { getDictionary,isLocale } from "@/i18n/dictionaries";
export default async function AuthPage({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const t=getDictionary(locale);return <main className="mx-auto max-w-md p-6"><h1 className="text-2xl font-bold">{t.auth.title}</h1><p role="status" className="my-4 rounded-lg bg-amber-50 p-3 text-amber-900">{t.auth.notConfigured}</p><form className="grid gap-3"><label htmlFor="email">{t.auth.emailLabel}</label><input disabled id="email" type="email" className="rounded-lg border p-3"/><button disabled className="rounded-lg bg-slate-300 p-3">{t.auth.email}</button><button disabled className="rounded-lg border p-3">{t.auth.google}</button></form></main>}
