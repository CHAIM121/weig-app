import type { Dictionary } from "@/i18n/dictionaries";
export type ViewState="loading"|"empty"|"error"|"offline"|"provider";
export function ModuleState({state,dictionary}:{state:ViewState;dictionary:Dictionary}){if(state==="loading")return <div role="status" aria-live="polite" className="animate-pulse rounded-xl border bg-white p-6">{dictionary.common.loading}</div>;const copy=state==="empty"?[dictionary.states.emptyTitle,dictionary.states.emptyBody]:state==="error"?[dictionary.states.errorTitle,dictionary.states.errorBody]:state==="offline"?[dictionary.states.offlineTitle,dictionary.states.offlineBody]:[dictionary.states.providerTitle,dictionary.states.providerBody];return <section role={state==="error"?"alert":"status"} className="rounded-xl border bg-white p-6"><h2 className="text-lg font-bold">{copy[0]}</h2><p className="mt-2 text-slate-600">{copy[1]}</p></section>}
