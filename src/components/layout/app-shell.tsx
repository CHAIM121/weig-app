import Link from "next/link";
import type { Dictionary, Locale } from "@/i18n/dictionaries";
import { localizePath } from "@/i18n/routing";
const destinations = ["places","expenses","calls","ai"] as const;
export function AppShell({children,locale,dictionary}:{children:React.ReactNode;locale:Locale;dictionary:Dictionary}){return <div className="min-h-screen bg-slate-50"><header className="sticky top-0 z-10 border-b bg-white px-4 py-4"><div className="mx-auto flex max-w-5xl items-center justify-between"><strong className="text-xl text-teal-800">{dictionary.common.appName}</strong><details className="relative"><summary className="cursor-pointer list-none rounded-lg border px-3 py-2">{dictionary.common.menu}</summary><aside className="absolute end-0 mt-2 w-48 rounded-xl border bg-white p-2 shadow-lg">{(["trips","profile","settings","signOut"] as const).map(item=><div className="rounded-lg px-3 py-2 text-sm text-slate-700" key={item}>{dictionary.nav[item]}</div>)}</aside></details></div></header><main className="safe-bottom mx-auto max-w-5xl p-4">{children}</main><nav aria-label="Primary" className="fixed inset-x-0 bottom-0 z-20 border-t bg-white pb-[env(safe-area-inset-bottom)]"><ul className="mx-auto grid max-w-lg grid-cols-4">{destinations.map(item=><li key={item}><Link className="flex min-h-16 items-center justify-center px-2 text-center text-sm font-medium text-slate-700 hover:bg-teal-50 focus-visible:outline-2 focus-visible:outline-teal-700" href={localizePath(locale,`/${item}`)}>{dictionary.nav[item]}</Link></li>)}</ul></nav></div>}
export { destinations };
